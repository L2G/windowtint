source 'https://rubygems.org'

# Specify your gem's dependencies in windowtint.gemspec
gemspec

group :development do
    gem 'rake', '~> 10.0'
    gem 'term-ansicolor'
    gem 'win32console', :platforms => [:mswin, :mingw]
end
